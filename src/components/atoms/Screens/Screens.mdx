import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import {getThemeVariables} from '../../utils-storybook'


export const Shadow = () => {
  const colorsList = getThemeVariables('color')
  const colors = [
    colorsList['accent-radiant'],
    colorsList['accent-rose'],
    colorsList['accent-purple'],
    colorsList['accent-pacific-blue'],
  ]


  const objectSpacing = getThemeVariables('breakpoint')
  const keysSpacing = Object.entries(objectSpacing).map(([k, v]) => v)
    console.log(objectSpacing, keysSpacing, 'colorsList')

  return <div className="flex flex-col gap-xs">
    {keysSpacing.map((key, index) => {
      const value = objectSpacing[key]

      return <div key={key} >
      <div className='text-center'>
      {key}
      </div>
      <div
        className="flex justify-center items-center text-content-fg-contrast desktop:bg-red-400"
         style={{
          width: value,
          height: '100px',
          backgroundColor: colors[index]
          }}>
        {`<=`} {value}
        </div>
        </div>
      })}
  </div>
}




## Border radius

Border radius usado no projeto

<Shadow />

