import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import {getThemeVariables} from '../../utils-storybook'

export const Shadow = () => {
    const colorsList = getThemeVariables('color')
  const colors = [
    colorsList['accent-radiant'],
  ]

    const shadowList = getThemeVariables('shadow')
  const keysSpacing = Object.entries(shadowList).map(([k,v]) => v)

  return <div className="flex gap-xs">
    {keysSpacing.map((key, index) => {
      const value = objectSpacing[key]

      return <div key={key} >
      <div className='text-center text-content-fg-subcontent'>
      {key}
      </div>
      <div
        className="flex justify-center items-center"
         style={{
          boxShadow: value,
          width: '100px',
          height: '100px'
          }}>



        </div>
        </div>
      })}
  </div>
}




## Sombras

Sombras usadas no projeto

<Shadow />

